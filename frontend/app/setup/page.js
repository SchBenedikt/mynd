import SetupWizard from '../../components/SetupWizard';

export const metadata = {
  title: 'MYND Setup',
  description: 'Ersteinrichtung für MYND'
};

export default function SetupPage() {
  return <SetupWizard />;
}